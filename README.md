
  # Create it

  This is a code bundle for Create it. The original project is available at https://www.figma.com/design/pQkA3eVml2nP9QdUPMumrY/Create-it.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  